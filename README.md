# 🍗 Restaurante IA Pro

Este es un sistema de punto de venta (POS) para restaurantes, mejorado con funciones de inteligencia artificial.

## Funcionalidades de IA (Opcional)

Para habilitar las funciones de inteligencia artificial (asistentes, generación de texto, etc.), necesitas una API Key de Google Gemini.

1.  Obtén una clave desde [Google AI Studio](https://aistudio.google.com/app/apikey).
2.  Inicia sesión en la aplicación con `admin` / `123`.
3.  Ve a `Configuración` > `IA & API`.
4.  Pega tu clave y haz clic en `Guardar y Recargar`. La aplicación se reiniciará con las funciones de IA activadas.